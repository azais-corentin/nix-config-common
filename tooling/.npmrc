# commitlint only needs its JS config loader; cosmiconfig-typescript-loader's
# typescript peer (a 27 MB native tsc) is never used. default.nix passes the
# same flag to the Nix build so the lockfile and node_modules agree.
legacy-peer-deps=true
